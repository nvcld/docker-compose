version: '3.1'

services:

  db:
    container_name: mariadb
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: $DB_PASS
      MARIADB_DATABASE: $DB_NAME

  adminer:
    container_name: adminer
    image: adminer
    restart: always
    ports:
      - 80:8080